export const todoReducer = (state = [], action) => {

    switch (action.type) {
        // case 'add':

        //     break;

        // case 'delete':

        //     break;

        default:
            return state;
            break;
    }

}